# План работ и разделение на троих

Контест: сегодня 23.09.2026, **судят версию на 18:00**. Коммит с рабочим промежуточным
результатом — **каждый час** (rule 5.4.8–5.4.11). Отметку `[x]` ставить тем же коммитом.

## Роли

| Разработчик | Зона | Модули |
| --- | --- | --- |
| **Dev A — Данные и погода** | Open-Meteo клиенты, кэш, агрегация датасета, фичи | `src/weather/`, `src/features/` |
| **Dev B — Модель** | baseline, LightGBM, holdout-валидация, метрики, бленд | `src/models/`, `src/backtest/` |
| **Dev C — Агент и сдача** | оркестратор Claude, CLI, README, submission, демо | `src/agent/`, `src/cli.py`, `README.md` |

Каждый работает через Claude Code в своих модулях. Чужие модули не трогаем; контракты —
в ARCHITECTURE.md, менять их — только голосом всем троим. Пушим в `main` часто и мелко,
перед пушем `git pull --rebase`. Конфликты исключаются границами модулей.

## Критический путь

`get_training_weather` (A) → обучающая матрица (A) → модель + метрика (B) → агент вокруг (C).
Пока A не отдал погоду, B работает на измеренном ветре из датасета как на суррогате прогноза
(интерфейс тот же), C строит каркас агента с mock-тулами. Никто никого не ждёт.

## Фаза 1 — каркас (до ~14:30)

- [ ] **A1.** `src/weather/openmeteo.py`: `get_training_weather()` — historical-forecast-api,
      часовые ветра 10/80/100/120 м + температура + давление, `timezone=Asia/Almaty`,
      кэш ответов в `data/weather_cache/*.json`. Скачать 2024-01→2026-01 для точки турбин.
- [ ] **A2.** `src/features/dataset.py`: `load_hourly_target()` — агрегация к часу по правилам
      DATA.md (≥4/6 десятиминуток, фильтр простоев ветер>4 и power=0).
- [ ] **B1.** `src/models/baseline.py`: кривая мощности (изотоническая регрессия или логистическая
      сигмоида wind→power) на измеренных данных. Скор на holdout 12.2025–01.2026 — **первая цифра
      MAE в чате команды**.
- [ ] **C1.** Каркас `src/agent/loop.py` + `src/cli.py` с mock-тулами; `uv`-проект, зависимости,
      `.env.example`. Прогон `run-agent --no-llm` на моках проходит конец-в-конец.

## Фаза 2 — основная модель и реальный агент (до ~16:00)

- [ ] **A3.** `get_archived_forecast()` — previous-runs-api, сшивка `_previous_day1` (часы 0–24)
      и `_previous_day2` (часы 24–48). Проверить на живом ответе риск №3 из RESEARCH.md.
- [ ] **A4.** `build_features()` по RESEARCH.md §3; тест сдвига корреляции таймзоны (ADR-006).
- [ ] **B2.** LightGBM на прогнозных фичах A; сравнение с baseline на holdout; сохранение
      артефактов в `models/artifacts/`. Отдельно метрики горизонтов 0–24 и 24–48.
- [ ] **B3.** Бленд baseline+LGBM, веса на holdout. Финальный `predict()`.
- [ ] **C2.** Реальные тулы в агенте, промпт цикла (получить→подготовить→прогнать→проверить→
      проанализировать→решить о пересчёте), отчёт `report_{date}.md` от агента.

## Фаза 3 — прогон теста и сдача (16:00–17:40)

- [ ] **C3.** Полный rolling-прогон 31.01→28.02: `run-agent --start 2026-01-31 --end 2026-02-28`.
      Выход: 29 дневных CSV + `forecasts/submission.csv`.
- [ ] **B4.** Санити сводного прогноза: распределение, суточный профиль, границы 0..1,
      сравнение февраля с january-holdout по стилю.
- [ ] **C4.** README для судей: установка, запуск, демо-сценарий, архитектура, скор на holdout.
      **Прогон с чистого клона на машине другого разработчика.**
- [ ] **Все.** 17:40 — фриз. Только README-правки. 18:00 — судят как есть.

## Если остаётся время (по одному, в порядке ценности)

- [ ] Мульти-модельные погодные фичи (ECMWF+GFS+ICON) — обычно −5–10% MAE.
- [ ] Квантильный прогноз (P10/P50/P90) LightGBM quantile — сильно для «оригинальности».
- [ ] Простая HTML-страница с графиком прогноза vs история (артефакт для презентации финала).
