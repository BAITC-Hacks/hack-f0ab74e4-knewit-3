# Текущие задачи и владельцы

Состояние от 23.09.2026. Результаты интеграции: [INTEGRATION](INTEGRATION.md).
Порядок работы: [GIT_WORKFLOW](GIT_WORKFLOW.md), приоритеты: [NEXT_STEPS](NEXT_STEPS.md).

## Объединённые изменения

| Ветка | Результат |
| --- | --- |
| `dev/forecast-availability` | Одинаковые признаки обучения и выпуска, происхождение строк, границы и пропуски архива, тесты и ограничения доступности |
| `dev/evaluation-protocol` | Ретроспективная оценка ансамбля, отдельные артефакты, CSV/JSON с периодами и метриками |
| `dev/judge-readiness` | Каталоги прогонов, защита отчётов, документация и уборка устаревших пояснений |
| `dev/evaluation-dashboard` | UI и PNG читают сохранённую оценку; метрики из CSV, lead 1/2, маски целей, отсечка обучения, фактическое покрытие P10–P90 |
| `dev/release-rehearsal` | Одна команда проверки сохранённого комплекта, Linux workflow, Docker без обучения при сборке и прогон без сети |

- [x] **Панель:** интегрированы оба режима и обработка отсутствующих/некорректных файлов;
  обновление перечитывает результаты, выбранная дата учитывает границы пользовательской оценки.
- [x] **Репетиция релиза:** интегрированы `scripts.reproduce`, защита канонических путей,
  сверка прогнозов и оценки, проверка неизменности файлов и отчёт о результатах.
- [x] **Историческая проверка LLM:** 28 завершённых выпусков без fallback сохранены
  в git-истории `4e6f7ce`. Их тексты относятся к прежним прогнозам.

## Приёмка общей версии

| Владелец | Задача | Состояние |
| --- | --- | --- |
| Интегратор | Округление общего стека признаков до 10 знаков, пересборка финальных и оценочных моделей, CSV, отчётов, manifest и иллюстраций | Выполняется; окончательные числа ещё не зафиксированы |
| Интегратор | Полные тесты общей версии, повторение 28 выпусков / 1344 turbine-hours на macOS и Linux без сети, допуск `1e-9`, запуск панели | Итоговая проверка ожидается; доказательства будут в INTEGRATION |
| Интегратор | Согласовать README и документацию с пересобранными артефактами, опубликовать общую версию | После итоговых проверок |

Новые канонические отчёты формируются с `--no-llm`, поскольку в среде пересборки нет
API-ключа. Старые LLM-объяснения не переносятся на новые прогнозы; прежний комплект
доступен в git-истории. После завершения приёмки её команды и результаты записываются
в [INTEGRATION](INTEGRATION.md).

## Оставшиеся задачи

| Приоритет | Владелец | Следующий результат |
| --- | --- | --- |
| 1 | Исследователь погодного архива | Час выпуска и проверяемая историческая доступность каждого входа; повторная оценка после изменения архива или lead |
| 2 | Владелец моделей | Калибровка P10–P90 на выделенном прошлом; измеренные покрытие и ширина по турбинам и lead |
| 3 | Владелец API-ключа | Свежий LLM-прогон по пересобранным моделям в отдельном каталоге, точный model ID и проверка объяснений по соответствующим CSV |
| Внешний блокер | Администратор организации | Разблокировать биллинг GitHub Actions и повторить Linux/Docker workflow |

Workflow уже интегрирован; блокировка биллинга не позволяет hosted-заданиям стартовать.
Его состояние и локальные результаты Linux описаны в [REPRODUCIBILITY](REPRODUCIBILITY.md).
Доказательство доступности погоды к часу выпуска остаётся отдельной исследовательской задачей.
