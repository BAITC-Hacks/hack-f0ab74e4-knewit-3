# Текущие задачи и разделение работы

План координации от 23.09.2026, база `60f50bb`. ТЗ: [CASE](CASE.md), обоснование
приоритетов: [NEXT_STEPS](NEXT_STEPS.md). Исторические планы каркаса сохранены в git;
этот файл описывает фактическое состояние и текущую волну разработки.

## Владельцы текущей волны

| Исполнитель | Ветка | Файлы и результат |
| --- | --- | --- |
| Agent 1 — доступность прогноза | `dev/forecast-availability` | `src/features/build.py`, `src/weather/openmeteo.py`, `tests/test_feature_availability.py`, новая заметка `docs/FEATURE_AVAILABILITY.md` |
| Agent 2 — оценка модели | `dev/evaluation-protocol` | `src/models/train.py`, `src/models/predict.py`, новый `src/backtest/evaluate.py` и необходимые новые помощники, `tests/test_evaluation_protocol.py`, новая заметка `docs/EVALUATION.md` |
| Интегратор — запуск и сдача | `dev/judge-readiness` | CLI, агент, UI, общая документация, уборка существующих экспериментов, интеграционные проверки |

Новые заметки Agent 1/2 появятся с их ветками. До получения commit SHA эти работы
не считаются завершёнными. Агенты не меняют `src/config.py`, зависимости и общие docs;
необходимые изменения передают интегратору. `prof_ideas.py` остаётся в уборке интегратора.

## Реализовано до текущей волны

- [x] Архив погоды: `get_weather()` и `get_issued_forecast()` через Previous Runs.
- [x] Почасовые цели: `load_hourly()`, фильтры полноты и предполагаемых простоев.
- [x] `build_features()`: погодные и аэродинамические признаки.
- [x] LightGBM, изотонический baseline, пять финальных моделей на турбину, квантили P10/P90.
- [x] Общий `DayGraph`: проверки переходов, резервный baseline, трасса и продолжение без LLM.
- [x] CLI, Streamlit-панель, Dockerfile и compose-конфигурация.
- [x] 28 выпусков 31.01–27.02.2026: 56 дневных CSV, 28 отчётов/трасс, подача на 1344 строки.
- [x] Командный LLM-прогон прежнего исполнителя (`e38bf10`); шаблон и граф проверены тестами.

## Текущая волна

- [ ] **Agent 1:** одинаковые входы по времени выпуска в обучении и ежедневном прогнозе;
  регрессии на полуночной границе, пропусках и краю архива; документированные ограничения API.
- [ ] **Agent 2:** ретроспективные fit/tune/evaluate периоды, оценка точного ансамбля,
  сохранённые прогнозы и метрики с происхождением модели.
- [x] **Интегратор:** отдельный каталог `--output-dir`, общий для CSV/отчётов/трасс,
  сравнения соседних выпусков и сборки submission; защита LLM-отчётов без ключа.
- [x] **Интегратор:** маршрут для судей, актуальные роли, границы канонических результатов,
  индекс экспериментов и уборка устаревших пояснений.

## Интеграция после handoff обоих агентов

- [ ] Проверить branch/SHA и состав файлов; объединить Agent 1 → Agent 2 → работу интегратора.
- [ ] Выполнить общий набор тестов; пересчитать ретроспективную оценку на исправленных входах.
- [ ] Подключить сохранённые оценочные прогнозы к панели/статическому графику, обновить цифры
  в README и исследовательском отчёте по одному подтверждённому источнику.
- [ ] Обучить финальные артефакты и создать один согласованный февральский прогон.
- [ ] Проверить новый граф с реальным API-ключом, подтверждая отсутствие скрытого fallback.
- [ ] Репетиция из чистого клона и контейнера, фиксация совместимых версий зависимостей.
- [ ] Финальная проверка подачи и обычный push проверенной интеграции в `main`.
