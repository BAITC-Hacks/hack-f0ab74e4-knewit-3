# Интеграция веток — 23.09.2026

Цель: объединить признаки по выпускам, ретроспективную оценку и воспроизводимый
запуск для судей. Основание: CASE, FEATURE_AVAILABILITY, EVALUATION и handoff команды.
Интеграционная ветка — `dev/integrate-forecast-evaluation`; результат опубликован
в `main` как `08ad340`. История обеих веток сохранена. Обновления перечислены ниже
по коммитам: исходная офлайн-проверка и последующий LLM-прогон — разные запуски.

## План и проверяемые результаты

- [x] Объединить `dev/judge-readiness` (`ab92f74`), PR #1 (`8e8ce8b`) и PR #2
  (`d263e83`). Базовая объединённая версия: 52 теста проходят.
- [x] Обучение: один `issued_feature_stack` на набор погоды, явная отсечка цели
  31.01.2026 23:00. Проверка: добавление февральских целей не меняет обучающие строки;
  `train_all` не повторяет построение стека для второй турбины.
- [x] Прогноз: отсутствующая ансамблевая скорость не передаётся в IsotonicRegression;
  результат остаётся невалидным и проходит общий путь проверки и отказа публикации.
- [x] Оценка: ни один выпуск не предшествует последнему дню обучения и настройки;
  нечисловые прогнозы не исчезают из метрик молча. Отчёт содержит параметры,
  сиды, признаки и версии. Пересчёт метрик выполняется из сохранённого CSV.
- [x] Зафиксировать проверенные зависимости, переобучить канонические модели,
  повторить оценку и полный февральский прогон на объединённых признаках.
- [x] Обновить документацию и сохранённые результаты одним согласованным комплектом.
  Проверить 1344 строки, 28 выпусков, обе турбины и панель. Повторить прогон без сети
  в чистом клоне. Отдельно указать, какие проверки среды не удалось выполнить.
- [x] Провести финальный обзор и подготовить публикацию обычным push в main. На Desktop
  созданы следующие независимые задания: `dev/evaluation-dashboard` и
  `dev/release-rehearsal`, каждое со своим worktree и точным владением файлами.

## Ограничения

Декабрь–январь уже влияли на решения команды: оценка ретроспективная, а не нетронутый
тест. Февральского факта нет. Previous Runs не хранит доказательство точного времени
публикации каждого значения; см. FEATURE_AVAILABILITY. Наличие Dockerfile само по себе
не доказывает прохождение контейнерного запуска.

## Исходная интеграция: проверки 08ad340

- Python 3.12.6, macOS arm64; установка зафиксированных прямых зависимостей в новую venv.
- Полный набор: **69 тестов**, без пропусков. Проверки Streamlit входят в этот запуск.
- Оценка: **5904 строки**, ни одного пропущенного выпуска; 48 строк раннего lead 2
  исключены по отсечке модели. Метрики повторно посчитаны из CSV и совпали с JSON.
- MAE на чистых целях: T1 0.1574 / 0.1729, T2 0.1574 / 0.1734 (lead 1 / 2).
  Покрытие P10–P90 — 0.6497 при номинале 0.8; интервал пока недокрывает факт.
- Оценка, переобучение и полный февральский прогон вместе заняли 113.2 с. Погодные
  HTTP-запросы были заблокированы; SHA256 raw-данных и кэша до/после совпали.
- Подача: **1344 строки**, 56 дневных CSV, 28 отчётов, 28 завершённых трасс.
  Исходный комплект создан в **no-llm**: ключ в этой среде отсутствовал. Старые
  LLM-отчёты сохранены в git-истории, их текст не перенесён к изменившимся числам.
- Финальный обзор обнаружил обработку полностью пропавшего дня как допустимого
  края архива. Исправлено: внутренний день остаётся 24 NaN-строками; выпуск без D+1
  за границей архива отклоняется. Три регрессионных случая прошли RED→GREEN.
- Версии, хэши входов, исходников и результатов: [manifest.json](../models_artifacts/manifest.json).

Docker в среде отсутствовал; реальный вызов LLM в этой репетиции не выполнялся.
Контейнер проверяет следующая волна; последующий LLM-запуск описан отдельно ниже.

## Чистый клон

Коммит `22cf7d2` проверен в новом клоне с новой `.venv`, установленной из requirements.txt:
69 тестов прошли без пропусков; все 28 выпусков воспроизведены с заблокированным
`socket.connect` и `httpx.get`. Все 1344 прогноза совпали с сохранённой подачей при
допуске 1e-9. Метрики повторно рассчитаны из оценочного CSV; SHA256 исходников, данных,
моделей и канонических прогнозов до/после совпали. Рабочее дерево клона осталось чистым.

Во время проверки main получил `ccd66f6` (слияние PR #3). Он включён в историю
интеграции; содержимое проверенного дерева при этом не изменилось.

## Канонические LLM-отчёты: 4e6f7ce

После интеграции участник команды опубликовал новые отчёты и трассы за все 28 выпусков.
В каждой трассе: `mode=openai`, `completed=true`, `fallback=false`, `retried=false`.
Автор коммита указал GPT-6 Luna; точный model ID и API-квитанция в трассе не сохраняются.
Подача и дневные CSV не изменились относительно `08ad340`.

Manifest согласован с новым комплектом отчётов. Хэши текущих исходников описывают
проверяемое дерево; исходная версия обучения и версия записанного LLM-запуска указаны
отдельно в `generation_provenance`. Более позднее уточнение промпта о доступности погоды
ещё не использовалось для генерации этих сохранённых текстов.

## Финальный аудит: dev/final-audit

- Удалены неиспользуемые импорты, исправлена аннотация `Path`, уточнены комментарии
  о фильтре простоев, периодах настройки, погодных срезах и диагностике времени.
  Численный алгоритм сохранён; промпт больше не утверждает доказанную доступность NWP.
- Документация отделяет выполненные задачи от текущей работы над UI и Docker.
  Аудит 76 погодных ответов и риск задержки публикации описаны в FEATURE_AVAILABILITY.
- **69 тестов прошли**, без пропусков. Полный запуск без LLM с заблокированными
  `socket.connect`, `socket.connect_ex` и `httpx.get`: **28 завершённых выпусков**,
  **1344 строки подачи**, 3.18 с в подготовленной среде macOS/Python 3.12.6.
- Все 57 CSV (56 дневных и submission) совпали с каноническими при абсолютном допуске
  1e-9. Хэши исходных данных, моделей и сохранённых прогнозов после проверки не изменились.
- Сохранённые LLM-трассы проверены на порядок шести шагов и успешность каждого шага;
  новый внешний вызов LLM в этом аудите не выполнялся.

Во время аудита в `main` появился README с иллюстрациями (`f9ae00a`). Его оформление
сохранено; утверждения о доступности погоды, автоматических обновлениях и качестве
уточнены. График настройки построен по текущему `train_report.json`; неподтверждённые
сравнения и причинные выводы удалены. Файлы агентов UI и Docker не изменены;
их результаты будут приняты после handoff и проверки общей версии.
